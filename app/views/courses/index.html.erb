<p id="notice"><%= notice %></p>

<h1 class="title-courses">Cursadas</h1>
<div class="container">
  <table class="table table-bordered table-light gradesTable table-striped ">
    
      <tr>
        <th>Año</th>
        <th>Nombre</th>
        <th colspan="3"></th>
      </tr>
      <% @courses.each do |course| %>
        <tr>
          <td><%= course.year %></td>
          <td><%= course.name %></td>
          <td class="col-center"><%= link_to 'Ver cursada', course %></td>
          <td class="col-center"><%= link_to  edit_course_path(course) do%>
            <%= content_tag :i,"", :class => "fa fa-edit" %>
            <% end %>
          </td>
          <td class="col-center"><%= link_to  course, method: :delete, data: { confirm: 'Are you sure?' } do %>
            <%= content_tag :i,"", :class => "fa fa-trash" %>
            <% end %>
          </td>
        </tr>
      <% end %>
  </table>

<%= link_to 'Nueva cursada', new_course_path, class: "btn btn-primary btn-form" %>
</div>
<br>
